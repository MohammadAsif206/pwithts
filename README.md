# 🎭 Playwright Automation Project (TypeScript)

This project contains end-to-end (E2E) test automation using **Playwright** with **TypeScript**, targeting the demo application:  
👉 https://valentinos-magic-beans.click

---

## 🚀 Project Overview

The goal of this project is to practice and demonstrate modern UI automation techniques using Playwright, including:

- Browser automation (Chromium, Firefox, WebKit)
- UI interaction and validation
- Reliable selectors and locators
- Test structuring and reusability
- Handling dynamic elements and waits
- Debugging and reporting

---

## 🛠️ Tech Stack

- Playwright  
- TypeScript  
- Node.js  
- VS Code  

---

## 📁 Project Structure
project-root/
│
├── tests/ # Test files
│ ├── example.spec.ts
│ └── ...
│
├── playwright.config.ts # Playwright configuration
├── package.json
├── tsconfig.json
└── README.md

---

## ⚙️ Setup Instructions
npm install

## Setup a project
Craete an empty folder, open a terminal in it
Run the command 'npm init -y' to initialize a node.js project
Run the command 'npm install @playwright/test' to install playwright
Run the command 'mpx playwright install' to install required browser binaries

## To generate code
Run the command 'npx playwright codegen'

### 1. Clone the Repository

```bash
git clone <your-repo-url>
cd <project-folder>
